name: secret-scan
on:
  push:
  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Gitleaks (optional)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: "--verbose"
